# VKhelper на BotHost с Kie.ai

Этот файл — шпаргалка для создания нового бота. Секреты не вставляйте в GitHub и не отправляйте в чат.

## 1. Основные поля BotHost

- Название: `VKhelper`
- Платформа: `VK`
- Язык: `Python`
- Версия Python: `3.11`
- Локация: `Нидерланды (Амстердам)`
- Bot Token: токен сообщества VK

## 2. Переменные окружения

Нажмите «Загрузить из файла .env» и выберите `bothost-kie.env` из папки `Загрузки/botVK`.

Перед загрузкой откройте `bothost-kie.env` в TextEdit и замените только это значение:

```text
KIE_API_KEY=PASTE_YOUR_KIE_KEY_HERE
```

на настоящий API-ключ из Kie.ai. Остальные значения уже перенесены из прежнего файла.

После загрузки убедитесь, что в BotHost есть:

```text
AI_PROVIDER=kie
KIE_API_KEY=ваш ключ Kie
KIE_MODEL=gpt-5-2
```

Строки Gemini можно не загружать: новый код их не использует.

## 3. Репозиторий

- Git URL: `https://github.com/itstxaviers-svg/VKhelper.git`
- Ветка: `main`
- Описание: `VK-бот для ответов клиентам клуба «Златоуст» с AI-помощником.`

Репозиторий публичный, поэтому ключ GitHub и SSH-доступ не нужны.

## 4. Дополнительные настройки

- Использовать домен: выключено
- Главный файл: `main.py`
- Использовать собственный Dockerfile: выключено

## 5. После создания

Нажмите «Создать бота», дождитесь статуса «Работает» и отправьте в сообщество тестовое сообщение «Привет».

Если BotHost снова покажет ошибку в логах, пришлите текст ошибки, но не токены.
